WITH TEMP AS
(
SELECT DISTINCT T.TEAM_ID
FROM KL_PLAYER P JOIN KL_TEAM T ON P.TEAM_ID = T.TEAM_ID
WHERE P.NATION = "브라질" OR P.NATION = "러시아"
)

SELECT TEMP.TEAM_ID, KL_TEAM.TEAM_NAME
FROM TEMP JOIN KL_TEAM ON TEMP.TEAM_ID = KL_TEAM.TEAM_ID
ORDER BY TEAM_ID
